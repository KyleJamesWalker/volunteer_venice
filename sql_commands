create table orgs(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name text not null,
    website text,
    address text,
    description text,
    category int,
    phone_number text,
    email text,
    picture_location text,
    video_location text
);
